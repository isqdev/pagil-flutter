import 'package:flutter/material.dart';

class FormTurma extends StatelessWidget {
  const FormTurma({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}